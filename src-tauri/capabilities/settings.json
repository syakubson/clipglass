{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings",
  "description": "Settings window — no paste or clipboard activate commands",
  "windows": ["settings"],
  "permissions": [
    "core:event:default",
    "opener:default",
    "settings-window-commands"
  ]
}
